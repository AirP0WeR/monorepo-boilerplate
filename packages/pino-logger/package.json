{
  "name": "@package/pino-logger",
  "main": "./index.ts",
  "types": "./index.ts",
  "source": "./index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -f *.tsbuildinfo"
  },
  "dependencies": {
    "pino": "^8.10.0",
    "@package/eslint-config-custom": "workspace:*"
  },
  "devDependencies": {
    "pino-pretty": "^9.2.0"
  }
}
